= bootstrap_form_for([ @article, @comment ], inline_errors: false) do |f|
  - if @comment.errors.any?
    - @comment.errors.full_messages.each do |msg|
      :javascript
        var msg = " #{msg} ";
        Command: toastr.error(msg);
  .col-lg-6
    = f.text_field :author_name, label: "Name", prepend: "<i class='fa fa-user'></i>".html_safe, required: true
  .col-lg-6
    = f.text_field :email, label: "Email", prepend: "<i class='fa fa-envelope-o'></i>".html_safe, required: true
  .col-lg-12
    = f.text_area :content, label: "Comment", :style => "min-height:100px"
  .box-footer.clearfix
    = f.submit 'Submit', :class => 'pull-right btn btn-primary'