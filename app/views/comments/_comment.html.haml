/ chat item
.item
  = image_tag "default_pic.png"
  %p.message
    %a.name{:href => "#"}
      %small.text-muted.pull-right
        %i.fa.fa-clock-o
        #{time_ago_in_words(comment.created_at)} ago
      #{comment.author_name}
    #{comment.content}
    %hr
/ /.item

