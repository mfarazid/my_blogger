!!!
%html
  %head
    %title MyBlogger
    = stylesheet_link_tag    "application", media: "all"
    = javascript_include_tag "application"
    = csrf_meta_tags
  %body
    / flash notifications
    - if flash[:toastr]
      - flash[:toastr].each do |type, message|
        :javascript
          Command: toastr.#{type}(#{raw(message.inspect)});

    .navbar.navbar-default.navbar-fixed-top{:role => "navigation"}
      .container
        .navbar-header
          %button.navbar-toggle{"data-target" => ".navbar-collapse", "data-toggle" => "collapse", :type => "button"}
            %span.sr-only Toggle navigation
            %span.icon-bar
            %span.icon-bar
            %span.icon-bar
          %a.navbar-brand{:href => root_path} MyBlogger
        .navbar-collapse.collapse
          %ul.nav.navbar-nav.navbar-right
            - if user_signed_in?
              - if current_user.authorizations.any?
                %li
                  = link_to "<i class='fa fa-edit'></i> Edit Profile".html_safe, edit_user_path(current_user)
                %li
                  = link_to "Logout (#{current_user.name})", destroy_user_session_path, method: :delete, :class => 'item'
              - else
                %li
                  = link_to "<i class='fa fa-edit'></i> Edit Profile".html_safe, edit_user_registration_path, :class => 'item'
                %li
                  = link_to "Logout (#{current_user.name})", destroy_user_session_path, method: :delete, :class => 'item'
            - else
              %li
                = link_to "Sign up", new_user_registration_path, :class => 'item'
              %li
                = link_to "Login", new_user_session_path, :class => 'item'
        / /.nav-collapse


    %section.container
      = yield
    %footer.footer
      %p
        Develop by Faraz Ali, all rights reserved